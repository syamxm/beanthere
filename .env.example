DB_HOST=db
DB_USER=beanthere
DB_PASS=change-me
DB_NAME=coffeebuddydb
DB_ROOT_PASS=change-me-too

# 1 on HTTPS deployments (adds the Secure flag to the session cookie).
# Leave 0 for local HTTP development, or the cookie is never sent.
SESSION_COOKIE_SECURE=0

# Local Ollama server used by the recommendation chatbot.
# Leave empty to always use the rule-based fallback.
OLLAMA_URL=http://172.31.0.1:11434
OLLAMA_MODEL=qwen2.5:3b-instruct
